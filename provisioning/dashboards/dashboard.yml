apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'Automated Dashboards'  # 대시보드가 저장될 폴더 이름
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards  # Grafana 컨테이너 내 대시보드 파일 경로
